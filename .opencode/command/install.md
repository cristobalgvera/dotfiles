---
description: Explain install flow and required steps for this repo
tags:
  - dotfiles
  - install
  - onboarding
dependencies:
  - context:dotfiles/guides/install-flow
  - context:dotfiles/quick-start
  - context:dotfiles/standards/tooling-policy
  - context:dotfiles/standards/secrets-local-overrides
---

# Dotfiles Install

**Purpose**: Guide through installation and configuration steps.

## What to do

1. Explain install flags and what they do.
2. Highlight macOS-only and zsh-only constraints.
3. Remind about `~/.zshrc.local` for secrets/overrides.
4. Provide the exact install command.

## Output format

- **Command**: single-line install command
- **Notes**: bullet list
